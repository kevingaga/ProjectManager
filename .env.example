# Copier ce fichier en .env et remplir les valeurs
# Ce fichier est gitignore — ne jamais committer .env

# Vercel token : vercel.com/account/tokens
VERCEL_TOKEN=

# Vercel Org ID : obtenu via `vercel link` → cat .vercel/project.json → orgId
VERCEL_ORG_ID=
